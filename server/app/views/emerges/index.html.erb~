<h1>Listing emerges</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Buildtime</th>
    <th>Duration</th>
    <th>Package</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @emerges.each do |emerge| %>
  <tr>
    <td><%= emerge.id %></td>
    <td><%= emerge.buildtime %></td>
    <td><%= emerge.duration %></td>
    <td><%= emerge.package %></td>
    <td><%= emerge.user %></td>
    <td><%= link_to 'Show', emerge %></td>
    <td><%= link_to 'Edit', edit_emerge_path(emerge) %></td>
    <td><%= link_to 'Destroy', emerge, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Emerge', new_emerge_path %>
