<h2><%= @pkgs.first.category %>/<%= @pkgs.first.name %></h2>

<% @pkgs.each do |pkg| %>
<h3><%= pkg.version %></h3>
<%= average(pkg) %>
<%= render(:partial => "emerges",
           :object => pkg.emerge,
           :locals => {:needuser => true}) %>
<% end %>
