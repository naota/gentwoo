<h1>GenTwoo: Socilal Compiling</h1>

<div>
  <%- if current_user -%>
  ようこそ<%= current_user.name =%>さん! 今日もコンパイルしてますか!?<br/>
  <a href="my">マイページに行く</a>/<a href="logout">ログアウト</a>
  <%- else -%>
  あなたもGenTwooでSocial Compileを始めませんか? 今すぐ<a href="login">Twitterでログイン</a>
  <%- end -%>
</div>

<h3>最近人気のpackage</h3>
<div>
<table>
<tr><td>回数</td><td>package</td></tr>
<% @poppkg.each do |pkg| %>
<tr>
  <td><%= pkg.cnt %></td>
  <td><%= link_to pkg.fullname, pkg.page %></td>
</tr>
<% end %>
</table>
</div>

<h3>最近よくemergeしている人</h3>
<div>
<table>
<tr><td>回数</td><td></td></tr>
<% @popperson.each do |p| %>
<tr>
  <td><%= p.cnt %></td>
  <td>
    <img src="<%= p.profile_image_url %>" style="vertical-align: middle" />
    <%= link_to p.name, p.link %>
  </td>
</tr>
<% end %>
</table>
</div>

<h3>最近のemerge</h3>
<div>
<%= render(:partial => "emerges",
           :object => @emerges,
           :locals => {:needuser => true}) %>
<a href="emerges/">もっと見る ...</a>
</div>
<h3>最近のエラーが出たemerge</h3>
<div>
<%= render(:partial => "emerges",
           :object => @erremerges,
           :locals => {:needuser => true}) %>
</div>
